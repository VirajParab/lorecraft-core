# LoreCraft Core — Python dependencies
# GPU packages (torch, vllm) are installed by component scripts.

# API / orchestration
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Hugging Face / models
huggingface-hub>=0.26.0
transformers>=4.46.0
safetensors>=0.4.0

# Audio utilities
soundfile>=0.12.0
librosa>=0.10.0
numpy>=1.26.0,<2

# CLI / utils
typer>=0.13.0
rich>=13.9.0

# Optional dev (see requirements-dev.txt)
# pytest, ruff, mypy
